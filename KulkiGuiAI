print("loading kulki GUI ai......")

local RunService = game:GetService("RunService")
local versionString = "VERSION 1.0"
local SavedCommunicator:BindableEvent = nil
if RunService:IsStudio() then
	repeat wait() until game:IsLoaded()
	game.Players.LocalPlayer:WaitForChild("PlayerGui")
	task.wait()
end
if game:FindFirstChild("GameNetworkingDebuggerV2") then
	if game:GetService("RunService"):IsStudio() then
		task.wait(9999999999999)
		game.Players.LocalPlayer:Kick("yielding ended.")
	end
	if game.Players.LocalPlayer.PlayerGui:FindFirstChild("kulkivsElliot") and game:FindFirstChild("GameNetworkingDebuggerV2"):FindFirstChild("Communicator") then
		game:FindFirstChild("GameNetworkingDebuggerV2"):FindFirstChild("Communicator"):Fire()
		task.wait(0.2)
	end
	if game.Players.LocalPlayer.PlayerGui:FindFirstChild("kulkivsElliot") then
		game.Players.LocalPlayer.PlayerGui:FindFirstChild("kulkiIvsElliot"):Destroy()
	end
	if game:FindFirstChild("GameNetworkingDebuggerV2"):FindFirstChild("Communicator") then
		SavedCommunicator = game:FindFirstChild("GameNetworkingDebuggerV2"):FindFirstChild("Communicator")
		SavedCommunicator:Fire()
	end
else
	local SavedData = Instance.new("Configuration")
	local Communicator = Instance.new("BindableEvent")
	local hard = Instance.new("BoolValue")
	hard.Name = "HARD"
	hard.Value = false
	Communicator.Name = "Communicator"
	SavedCommunicator = Communicator
	Communicator.Parent = SavedData
	SavedData.Name = "GameNetworkingDebuggerV2" -- shhhhh, its a disguise.
	SavedData.Parent = game	
end
local Client = game.Players.LocalPlayer
local UIS =  game:GetService("UserInputService")
local mouse = Client:GetMouse()
local mouseIsBeingHeld = false

--very cool ui part

local sGUI = Instance.new("ScreenGui")
sGUI.ResetOnSpawn = false
local MainFrame = Instance.new("Frame")
MainFrame.AnchorPoint = Vector2.new(0.5,0.5)
MainFrame.Parent = sGUI
MainFrame.Position = UDim2.new(0.5, 0,0.5, 0)
MainFrame.Size = UDim2.new(0.753, 0,0.688, 0)
MainFrame.BackgroundTransparency = 0.05
MainFrame.BorderSizePixel = 0
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
local UIdrag = Instance.new("UIDragDetector")
UIdrag.Parent = MainFrame
local corner = Instance.new("UICorner") corner.CornerRadius = UDim.new(0.02,0) corner.Parent = MainFrame
sGUI.Name = "kulkivsElliot"

local FR_tabs = Instance.new("Frame")
FR_tabs.Parent = MainFrame
FR_tabs.Position = UDim2.new(0, 0,0.037, 0)
FR_tabs.Size = UDim2.new(0.189, 0,0.963, 0)
FR_tabs.BackgroundTransparency = 1
FR_tabs.Parent = MainFrame
FR_tabs.BorderSizePixel = 0
local layout = Instance.new("UIListLayout")
layout.Wraps = true
layout.FillDirection = Enum.FillDirection.Horizontal
layout.Parent = FR_tabs
local line = Instance.new("Frame")
line.BorderSizePixel = 0
line.BackgroundColor3 = Color3.fromRGB(15,15,15)
line.Size = UDim2.new(0.013, 0,1, 0)
line.Position = UDim2.new(0.188, 0,0, 0)
line.Parent = MainFrame
local FR_options = Instance.new("ScrollingFrame")
FR_options.Position = UDim2.new(0.201, 0,0.184, 0)
FR_options.BackgroundTransparency = 1
FR_options.Size = UDim2.new(0.799, 0,0.814, 0)
FR_options:AddTag("Tab")
FR_options.ScrollBarThickness = 12
FR_options.ScrollBarImageTransparency = 0.5
FR_options.CanvasSize = UDim2.new(0, 0,50, 0)
layout:Clone().Parent = FR_options

local KGM = Instance.new("TextLabel")
KGM.Text = "K"
KGM.TextTransparency = 0.5
KGM.TextColor3 = Color3.fromRGB(255,255,255)
KGM.BackgroundColor3 = Color3.fromRGB(0,0,0)
KGM.BackgroundTransparency = 0.5
KGM.TextScaled = true
KGM.Font = Enum.Font.SourceSansItalic
UIdrag:Clone().Parent = KGM
KGM.Visible = false
local hitbox = Instance.new("TextButton")
hitbox.Size = UDim2.new(0.5, 0,0.5, 0)
hitbox.Position = UDim2.new(0.257, 0,0.243, 0)
hitbox.BackgroundTransparency = 1
hitbox.Text = ""
hitbox.Parent = KGM
hitbox.MouseButton1Click:Connect(function()
	KGM.Visible = false
	MainFrame.Visible = true
end)
KGM.Parent = sGUI
KGM.Position = UDim2.new(0.465, 0,0.193, 0)
KGM.Size = UDim2.new(0.071, 0,0.126, 0)

local disableKulkiGUI = Instance.new("TextButton")
disableKulkiGUI.Size = UDim2.new(0.054, 0,0.105, 0)
disableKulkiGUI.Position = UDim2.new(0.949, 0,0.01, 0)
disableKulkiGUI.Text = "X"
disableKulkiGUI.BackgroundColor3 = Color3.fromRGB(223, 223, 223)
disableKulkiGUI.TextColor3 = Color3.fromRGB(255,0,0)
disableKulkiGUI.TextScaled = true
local minimizeKulkiGui = disableKulkiGUI:Clone()
minimizeKulkiGui.Position = UDim2.new(0.856, 0,0.01, 0)
minimizeKulkiGui.Text = "-"
minimizeKulkiGui.TextColor3 = Color3.fromRGB(0,0,0)

minimizeKulkiGui.Parent = MainFrame
disableKulkiGUI.Parent = MainFrame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(0.797, 0,0.129, 0)
title.Position = UDim2.new(0.201, 0,0.01, 0)
title.Text = "Kulki GUI"
title.TextColor3 = Color3.fromRGB(255,255,255)
title.BackgroundTransparency = 1
title.TextScaled = true
title.Font = Enum.Font.SourceSans
title.Parent = MainFrame
local subtitle = title:Clone()
subtitle.Text = versionString
subtitle.Size = UDim2.new(0.799, 0,0.045, 0)
subtitle.Position = UDim2.new(0.201, 0,0.139, 0)
subtitle.Parent = MainFrame
subtitle.Font = Enum.Font.SourceSansBold

--actual kulki gui code below

--SIMPLEPATH MODULE
--[[
-------------------------------------------------------------------

Created by: @V3N0M_Z
Reference: https://v3n0m-z.github.io/RBLX-SimplePath/
License: MIT

---------------------------------------------------------------------
]]

local DEFAULT_SETTINGS = {

	TIME_VARIANCE = 0.07;

	COMPARISON_CHECKS = 1;

	JUMP_WHEN_STUCK = true;
}

---------------------------------------------------------------------

local PathfindingService = game:GetService("PathfindingService")
local Players = game:GetService("Players")
local function output(func, msg)
	func(((func == error and "SimplePath Error: ") or "SimplePath: ")..msg)
end
local Path = {
	StatusType = {
		Idle = "Idle";
		Active = "Active";
	};
	ErrorType = {
		LimitReached = "LimitReached";
		TargetUnreachable = "TargetUnreachable";
		ComputationError = "ComputationError";
		AgentStuck = "AgentStuck";
	};
}
Path.__index = function(table, index)
	if index == "Stopped" and not table._humanoid then
		output(error, "Attempt to use Path.Stopped on a non-humanoid.")
	end
	return (table._events[index] and table._events[index].Event)
		or (index == "LastError" and table._lastError)
		or (index == "Status" and table._status)
		or Path[index]
end

--Used to visualize waypoints
local visualWaypoint = Instance.new("Part")
visualWaypoint.Size = Vector3.new(0.3, 0.3, 0.3)
visualWaypoint.Anchored = true
visualWaypoint.CanCollide = false
visualWaypoint.Material = Enum.Material.Neon
visualWaypoint.Shape = Enum.PartType.Ball

--[[ PRIVATE FUNCTIONS ]]--
local function declareError(self, errorType)
	self._lastError = errorType
	self._events.Error:Fire(errorType)
end

--Create visual waypoints
local function createVisualWaypoints(waypoints)
	local visualWaypoints = {}
	for _, waypoint in ipairs(waypoints) do
		local visualWaypointClone = visualWaypoint:Clone()
		visualWaypointClone.Position = waypoint.Position
		visualWaypointClone.Parent = workspace
		visualWaypointClone.Color =
			(waypoint == waypoints[#waypoints] and Color3.fromRGB(0, 255, 0))
			or (waypoint.Action == Enum.PathWaypointAction.Jump and Color3.fromRGB(255, 0, 0))
			or Color3.fromRGB(255, 139, 0)
		table.insert(visualWaypoints, visualWaypointClone)
	end
	return visualWaypoints
end

--Destroy visual waypoints
local function destroyVisualWaypoints(waypoints)
	if waypoints then
		for _, waypoint in ipairs(waypoints) do
			waypoint:Destroy()
		end
	end
	return
end

--Get initial waypoint for non-humanoid
local function getNonHumanoidWaypoint(self)
	--Account for multiple waypoints that are sometimes in the same place
	for i = 2, #self._waypoints do
		if (self._waypoints[i].Position - self._waypoints[i - 1].Position).Magnitude > 0.1 then
			return i
		end
	end
	return 2
end

--Make NPC jump
local function setJumpState(self)
	pcall(function()
		if self._humanoid:GetState() ~= Enum.HumanoidStateType.Jumping and self._humanoid:GetState() ~= Enum.HumanoidStateType.Freefall then
			self._humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
		end
	end)
end

--Primary move function
local function move(self)
	if self._waypoints[self._currentWaypoint].Action == Enum.PathWaypointAction.Jump then
		setJumpState(self)
	end
	self._humanoid:MoveTo(self._waypoints[self._currentWaypoint].Position)
end

--Disconnect MoveToFinished connection when pathfinding ends
local function disconnectMoveConnection(self)
	self._moveConnection:Disconnect()
	self._moveConnection = nil
end

--Fire the WaypointReached event
local function invokeWaypointReached(self)
	local lastWaypoint = self._waypoints[self._currentWaypoint - 1]
	local nextWaypoint = self._waypoints[self._currentWaypoint]
	self._events.WaypointReached:Fire(self._agent, lastWaypoint, nextWaypoint)
end

local function moveToFinished(self, reached)

	--Stop execution if Path is destroyed
	if not getmetatable(self) then return end

	--Handle case for non-humanoids
	if not self._humanoid then
		if reached and self._currentWaypoint + 1 <= #self._waypoints then
			invokeWaypointReached(self)
			self._currentWaypoint += 1
		elseif reached then
			self._visualWaypoints = destroyVisualWaypoints(self._visualWaypoints)
			self._target = nil
			self._events.Reached:Fire(self._agent, self._waypoints[self._currentWaypoint])
		else
			self._visualWaypoints = destroyVisualWaypoints(self._visualWaypoints)
			self._target = nil
			declareError(self, self.ErrorType.TargetUnreachable)
		end
		return
	end

	if reached and self._currentWaypoint + 1 <= #self._waypoints  then --Waypoint reached
		if self._currentWaypoint + 1 < #self._waypoints then
			invokeWaypointReached(self)
		end
		self._currentWaypoint += 1
		move(self)
	elseif reached then --Target reached, pathfinding ends
		disconnectMoveConnection(self)
		self._status = Path.StatusType.Idle
		self._visualWaypoints = destroyVisualWaypoints(self._visualWaypoints)
		self._events.Reached:Fire(self._agent, self._waypoints[self._currentWaypoint])
	else --Target unreachable
		disconnectMoveConnection(self)
		self._status = Path.StatusType.Idle
		self._visualWaypoints = destroyVisualWaypoints(self._visualWaypoints)
		declareError(self, self.ErrorType.TargetUnreachable)
	end
end

--Refer to Settings.COMPARISON_CHECKS
local function comparePosition(self)
	if self._currentWaypoint == #self._waypoints then return end
	self._position._count = ((self._agent.PrimaryPart.Position - self._position._last).Magnitude <= 0.07 and (self._position._count + 1)) or 0
	self._position._last = self._agent.PrimaryPart.Position
	if self._position._count >= self._settings.COMPARISON_CHECKS then
		if self._settings.JUMP_WHEN_STUCK then
			setJumpState(self)
		end
		declareError(self, self.ErrorType.AgentStuck)
	end
end

--[[ STATIC METHODS ]]--
function Path.GetNearestCharacter(fromPosition)
	local character, dist = nil, math.huge
	for _, player in ipairs(Players:GetPlayers()) do
		if player.Character and (player.Character.PrimaryPart.Position - fromPosition).Magnitude < dist then
			character, dist = player.Character, (player.Character.PrimaryPart.Position - fromPosition).Magnitude
		end
	end
	return character
end

--[[ CONSTRUCTOR ]]--
function Path.new(agent, agentParameters, override)
	if not (agent and agent:IsA("Model") and agent.PrimaryPart) then
		output(error, "Pathfinding agent must be a valid Model Instance with a set PrimaryPart.")
	end

	local self = setmetatable({
		_settings = override or DEFAULT_SETTINGS;
		_events = {
			Reached = Instance.new("BindableEvent");
			WaypointReached = Instance.new("BindableEvent");
			Blocked = Instance.new("BindableEvent");
			Error = Instance.new("BindableEvent");
			Stopped = Instance.new("BindableEvent");
		};
		_agent = agent;
		_humanoid = agent:FindFirstChildOfClass("Humanoid");
		_path = PathfindingService:CreatePath(agentParameters);
		_status = "Idle";
		_t = 0;
		_position = {
			_last = Vector3.new();
			_count = 0;
		};
	}, Path)

	--Configure settings
	for setting, value in pairs(DEFAULT_SETTINGS) do
		self._settings[setting] = self._settings[setting] == nil and value or self._settings[setting]
	end

	--Path blocked connection
	self._path.Blocked:Connect(function(...)
		if (self._currentWaypoint <= ... and self._currentWaypoint + 1 >= ...) and self._humanoid then
			setJumpState(self)
			self._events.Blocked:Fire(self._agent, self._waypoints[...])
		end
	end)

	return self
end


--[[ NON-STATIC METHODS ]]--
function Path:Destroy()
	for _, event in ipairs(self._events) do
		event:Destroy()
	end
	self._events = nil
	if rawget(self, "_visualWaypoints") then
		self._visualWaypoints = destroyVisualWaypoints(self._visualWaypoints)
	end
	self._path:Destroy()
	setmetatable(self, nil)
	for k, _ in pairs(self) do
		self[k] = nil
	end
end

function Path:Stop()
	if not self._humanoid then
		output(error, "Attempt to call Path:Stop() on a non-humanoid.")
		return
	end
	if self._status == Path.StatusType.Idle then
		output(function(m)
			warn(debug.traceback(m))
		end, "Attempt to run Path:Stop() in idle state")
		return
	end
	disconnectMoveConnection(self)
	self._status = Path.StatusType.Idle
	self._visualWaypoints = destroyVisualWaypoints(self._visualWaypoints)
	self._events.Stopped:Fire(self._model)
end

function Path:Run(target)

	--Non-humanoid handle case
	if not target and not self._humanoid and self._target then
		moveToFinished(self, true)
		return
	end

	--Parameter check
	if not (target and (typeof(target) == "Vector3" or target:IsA("BasePart"))) then
		output(error, "Pathfinding target must be a valid Vector3 or BasePart.")
	end

	--Refer to Settings.TIME_VARIANCE
	if os.clock() - self._t <= self._settings.TIME_VARIANCE and self._humanoid then
		task.wait(os.clock() - self._t)
		declareError(self, self.ErrorType.LimitReached)
		return false
	elseif self._humanoid then
		self._t = os.clock()
	end

	--Compute path
	local pathComputed, _ = pcall(function()
		self._path:ComputeAsync(self._agent.PrimaryPart.Position, (typeof(target) == "Vector3" and target) or target.Position)
	end)

	--Make sure path computation is successful
	if not pathComputed
		or self._path.Status == Enum.PathStatus.NoPath
		or #self._path:GetWaypoints() < 2
		or (self._humanoid and self._humanoid:GetState() == Enum.HumanoidStateType.Freefall) then
		self._visualWaypoints = destroyVisualWaypoints(self._visualWaypoints)
		task.wait()
		declareError(self, self.ErrorType.ComputationError)
		return false
	end

	--Set status to active; pathfinding starts
	self._status = (self._humanoid and Path.StatusType.Active) or Path.StatusType.Idle
	self._target = target

	--Set network owner to server to prevent "hops"
	pcall(function()
		self._agent.PrimaryPart:SetNetworkOwner(nil)
	end)

	--Initialize waypoints
	self._waypoints = self._path:GetWaypoints()
	self._currentWaypoint = 2

	--Refer to Settings.COMPARISON_CHECKS
	if self._humanoid then
		comparePosition(self)
	end

	--Visualize waypoints
	destroyVisualWaypoints(self._visualWaypoints)
	self._visualWaypoints = (self.Visualize and createVisualWaypoints(self._waypoints))

	--Create a new move connection if it doesn't exist already
	self._moveConnection = self._humanoid and (self._moveConnection or self._humanoid.MoveToFinished:Connect(function(...)
		moveToFinished(self, ...)
	end))

	--Begin pathfinding
	if self._humanoid then
		self._humanoid:MoveTo(self._waypoints[self._currentWaypoint].Position)
	elseif #self._waypoints == 2 then
		self._target = nil
		self._visualWaypoints = destroyVisualWaypoints(self._visualWaypoints)
		self._events.Reached:Fire(self._agent, self._waypoints[2])
	else
		self._currentWaypoint = getNonHumanoidWaypoint(self)
		moveToFinished(self, true)
	end
	return true
end
local SimplePath = Path

--KULKI GUI ai

minimizeKulkiGui.MouseButton1Click:Connect(function()
	KGM.Visible = true
	MainFrame.Visible = false
end)

local canQuit = false
local VIM = game:GetService("VirtualInputManager")
local function keyPress(key) --key has to be enum!
	VIM:SendKeyEvent(true,key,false,game)
	task.wait(0.005)
	VIM:SendKeyEvent(false,key,false,game)
end
local function keyHold(key,tim) --key has to be enum!
	VIM:SendKeyEvent(true,key,false,game)
	task.wait(tim)
	VIM:SendKeyEvent(false,key,false,game)
end
local function keyHoldRelease(key) --key has to be enum!
	VIM:SendKeyEvent(false,key,false,game)
end
--keyHold(Enum.KeyCode.LeftShift,5)



local canRunUpdateLoop = true
local tempDisable = false
local options = {
	["Enabled"] = false,
}
local DEBUGoptions = {
	["IngameCHILDADDED"] = false,
	["IngameDESCENDANTADDED"] = false,
}
local skinStatus = {
	["BigDjohnDoe"] = false,
	["GyatCoolkid"] = false,
	["NaziChance"] = false,
}

local function createOption(displayed:string,OptionType:string,OptionIndex:string,ForTab:string,default:string,FunctionToggle:boolean,Func,onlyOnMap:string,arg1:any)
	local relbt = nil
	local function can()
		if onlyOnMap then
			if workspace:FindFirstChild("Map"):FindFirstChild("Ingame"):FindFirstChild("Map") and workspace:FindFirstChild("Map"):FindFirstChild("Ingame"):FindFirstChild("Map"):GetAttribute("MapName") == onlyOnMap then
				return true
			else
				return false
			end
		else
			return true
		end
	end
	if OptionType:lower() == "toggle" then
		local btt = Instance.new("TextButton")
		btt.Size = UDim2.new(1, 0,0.003, 0)
		if not default then
			default = "OFF"
		end
		btt.Text = displayed .. " (" .. default .. ")"
		btt.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
		btt.Parent = MainFrame:FindFirstChild(ForTab)
		btt.TextScaled = true
		btt.MouseButton1Click:Connect(function()
			if can() == true then
				if FunctionToggle == true then
					options[OptionIndex] = not options[OptionIndex]
				end
				local to = " (ON)"
				if options[OptionIndex] == false then
					to = " (OFF)"
				end
				if Func then
					Func(arg1)
				end
				btt.Text = displayed .. to				
			end
		end)
		relbt = btt
	elseif OptionType:lower() == "use" then
		local btt = Instance.new("TextButton")
		btt.Size = UDim2.new(1, 0,0.003, 0)
		btt.Text = displayed
		btt.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
		btt.Parent = MainFrame:FindFirstChild(ForTab)
		btt.TextScaled = true
		btt.MouseButton1Click:Connect(function()
			if can() == true then
				print("YES")
				if Func then
					print("HOLDUP")
					Func(arg1)
				end				
			end
		end)
		relbt = btt
	end
end
local function addTabToUI(layoutOrder:number,Background:Color3,text:string,SetsTo:string)
	local btt = Instance.new("TextButton")
	btt.LayoutOrder = layoutOrder
	btt.Size = UDim2.new(0.496, 0,0.184, 0)
	btt.BackgroundColor3 = Background
	btt.Text = text
	btt.Parent = FR_tabs
	btt.TextScaled = true
	local tab = FR_options:Clone()
	tab.Parent = MainFrame
	tab.Name = SetsTo
	btt.MouseButton1Click:Connect(function()
		for i,tob in MainFrame:GetChildren() do
			if tob:HasTag("Tab") then
				tob.Visible = false
			end
		end
		tab.Visible = true
	end)
	return {["Button"]=btt,["Tab"]=tab}
end

if RunService:IsStudio() then
	addTabToUI(1,Color3.fromRGB(255,255,255),"General Cheats","GeneralCheats")
	addTabToUI(2,Color3.fromRGB(255,255,255),"Misc","Misc")
	createOption("TEST 01","toggle","UnUsedValue","GeneralCheats",nil,true)
	createOption("USE TEST","use",nil,"GeneralCheats",nil,nil,nil)
	sGUI.Parent = Client.PlayerGui
	assert(false," rest of kulki gui code cannot run in studio!")
end


local optionButtons = {}

local limiter = nil



local function FULLSTOP()
	canQuit = true
	for i,opt in options do
		opt = false
	end
	canRunUpdateLoop = false
	sGUI:Destroy()
	task.wait(99999999)
end


disableKulkiGUI.MouseButton1Click:Connect(function()
	FULLSTOP()
end)
task.wait(0.05)
if SavedCommunicator ~= nil then
	SavedCommunicator.Event:Connect(function()
		FULLSTOP()
	end)	
end


local speedingGen = false
local function GetClosestSurvivor(returnFormated)
	local nearest = {
		["Survivor"] = nil,
		["Magnitude"] = 10000
	}
	for i,survivor in workspace:FindFirstChild("Players"):FindFirstChild("Survivors"):GetChildren() do
		if survivor ~= Client.Character and survivor:FindFirstChild("HumanoidRootPart") and survivor:FindFirstChildWhichIsA("Humanoid") and survivor:FindFirstChildWhichIsA("Humanoid").Health > 0 then
			local mag = (survivor:FindFirstChild("HumanoidRootPart").Position-Client.Character:FindFirstChild("HumanoidRootPart").Position).Magnitude
			if mag < nearest.Magnitude then
				nearest.Magnitude = mag
				nearest.Survivor = survivor
			end
		end
	end
	if returnFormated == true then
		return nearest
	else
		return nearest.Survivor
	end
end

local poth = SimplePath.new(game.Players.LocalPlayer.Character)
poth.Visualize = true
local Data = {
	["Goal"] = nil,
	["Target"] = nil,
}
local ForsakenModules = {
	["Systems"] = {
		["Sprinting"] = require(game.ReplicatedStorage:WaitForChild("Systems"):WaitForChild("Character"):WaitForChild("Game"):WaitForChild("Sprinting"))
	}
}
game.Players.LocalPlayer.CharacterAdded:Connect(function(char)
	if canRunUpdateLoop==true then
		poth = SimplePath.new(char)
	end
end)
local function onMapStart()
	print("maper stater")
	task.wait(3)
	for i,survivor in workspace:FindFirstChild("Players"):FindFirstChild("Survivors"):GetChildren() do
		Data.Target = GetClosestSurvivor(false)
	end
end
workspace:FindFirstChild("Map"):FindFirstChild("Ingame").ChildAdded:Connect(function(kid)
	if kid.Name == "Map" then
		onMapStart()
	end
end)
if workspace:FindFirstChild("Map"):FindFirstChild("Ingame"):FindFirstChild("Map") then
	onMapStart()
end
task.spawn(function() --main update loop
	while true do
		task.wait()
		print("im mad and im ai")
		if canRunUpdateLoop == false then
			break
		end
		local su,err = nil
		su,err = pcall(function()
			local currentValues = {
				["Stamina"] = ForsakenModules.Systems.Sprinting.Stamina
			}
			if currentValues.Stamina>10 and ForsakenModules.Systems.Sprinting.CanSprint == true then
				ForsakenModules.Systems.Sprinting.IsSprinting = true
			else
				ForsakenModules.Systems.Sprinting.IsSprinting = false
			end
			if Data.Target then
				Data.Goal = Data.Target:FindFirstChild("HumanoidRootPart")
			else
				Data.Goal = nil
			end
			if Data.Goal then
				print("try and block this!")
				poth:Run(Data.Goal)
			end
		end)
		if err then
			warn("OH MY GOOOOOOOD!!!!, WADAFAK!!!!! , " .. tostring(err))
		end
	end
end)
local function noBlur()
	if options.NoBlur == false then
		options.NoBlur = true
		for i,blur in game.Lighting:GetDescendants() do
			if blur:IsA("BlurEffect") then
				blur.Size = 0
				blur.Enabled = false
			end
		end
	else
		options.NoBlur = false
	end
end



local packed = false
local function HasCharacter(char:string)
	local returnin = false
	if game:GetService("Players").LocalPlayer.PlayerData.Purchased.Killers:FindFirstChild(char) or game:GetService("Players").LocalPlayer.PlayerData.Purchased.Survivors:FindFirstChild(char) then
		returnin = true
	end
	return returnin
end
local function PatchLockedCharacters()
	if packed == false then
		options.UnUsedValue = false
		local f = false
		for i,char in game:GetService("ReplicatedStorage").Assets.Killers:GetChildren() do
			if string.find(char.Name,"#")  then
				f = true
				break
			end
		end
		if f == true then	
			for i,char in game:GetService("ReplicatedStorage").Assets.Killers:GetChildren() do
				if string.find(char.Name,"#")  then
					char.Name = string.gsub(char.Name, "#", "")
				end
			end
			for i,char in game:GetService("ReplicatedStorage").Assets.Survivors:GetChildren() do
				if string.find(char.Name,"#")  then
					char.Name = string.gsub(char.Name, "#", "")
				end
			end	
		end
		packed = true
	end
end	

local function GetCharacter(char:string,xp:number,killer:boolean)
	local int = Instance.new("IntValue")
	int.Value = xp
	int.Name = char
	if killer then
		int.Parent = game:GetService("Players").LocalPlayer.PlayerData.Purchased.Killers	
	else
		int.Parent = game:GetService("Players").LocalPlayer.PlayerData.Purchased.Survivors
	end
end

local function giveSkin(skinName)
	local int = Instance.new("IntValue")
	int.Value = 0
	int.Name = skinName
	int.Parent = game.Players.LocalPlayer.PlayerData.Purchased.Skins	
end

addTabToUI(1,Color3.fromRGB(255,255,255),"General","General")
--visual
createOption("AI ENABLED","toggle","Enabled","General",nil,true)


task.spawn(function()
	PatchLockedCharacters()
	task.wait(3)
	GetCharacter("Guest1",math.random(0,18000),true)
end)

if game:FindFirstChild("GameNetworkingDebugger") and game:FindFirstChild("GameNetworkingDebugger"):FindFirstChild("HARD") and game:FindFirstChild("GameNetworkingDebugger"):FindFirstChild("HARD").Value == true then
	FULLSTOP()
end


for i,tab in MainFrame:GetChildren() do
	if tab:HasTag("Tab") and tab:IsA("ScrollingFrame") then
		tab.CanvasSize = UDim2.new(0, 0,50, 0)
	end
end

print("loaded kulki GUI!")
sGUI.Parent = Client.PlayerGui
task.wait(3)


--https://raw.githubusercontent.com/BBro2137/KulkiGUI/refs/heads/main/source
--loadstring(game:HttpGet('https://raw.githubusercontent.com/BBro2137/KulkiGUI/refs/heads/main/source'))()

--m1 sound id's

--rbxassetid://101199185291628 --1x
--rbxassetid://140242176732868 --john doe
--rbxassetid://102228729296384 --coolkid
--rbxassetid://112809109188560 --jason
